{
  "name": "sun",
  "version": "1.0.0",
  "description": "人工智能",
  "main": "index.js",
  "scripts": {
    "gulp:build": "gulp",
    "dev": "gulp && gulp watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "sun",
  "dependencies": {
    "vant-weapp": "^0.5.28"
  },
  "devDependencies": {

  }
}
